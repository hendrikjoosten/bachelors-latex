%defining the type of documents
\documentclass[a4paper,12pt]{report}

%made by
\author{Hendrik Joosten}

%very inportant basic commands in latex
\usepackage{titlesec}

%so that i may include figures.
\usepackage{graphicx}

%so that i may use some math symbols and equations and stuff.
\usepackage{amsmath, amsthm, amssymb}


\usepackage[subrefformat=parens,labelformat=parens]{subfig}


\usepackage{lscape}
\usepackage[none]{hyphenat}
\usepackage{setspace}
\usepackage{xcolor}
\usepackage{bm}                                             

\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{cite}


\usepackage{longtable}

%using normal bullet lists
\usepackage{enumitem}

%this package sets up the page parameters
\usepackage[top = 1in, bottom = 1in, left = 1in, right = 1in]{geometry}

%package that doesnt mess up the numbering when images referenced
\usepackage{notoccite}

%making the captions look better
\usepackage{caption}

%important for centering all the images
\usepackage{floatrow}

%set the line spacing to like 1.5 equavaent on word
\linespread{1.25}





\usepackage[bookmarks         = true      ,%
            bookmarksnumbered = true      ,%
            setpagesize       = false     ,%
            colorlinks        = true      ,%
            linkcolor         = black     ,%
            urlcolor          = black     ,%
            citecolor         = black     ,%
            pdfpagelayout     = OneColumn ,%
            pdfstartview      = FitH      ,%
            driverfallback    = dvipdfm]{hyperref}
            
            
%should make headers and footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\lhead{Chapter \thechapter}
\rhead{Section \thesection}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}
\setlength{\headheight}{15pt} 

%define paragraph skips and indents
\parskip = 4mm
\parindent = 0mm




         

